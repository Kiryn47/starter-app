server {
    listen 80;
    resolver 127.0.0.11 valid=5s ipv6=off;
    set $upstream http://app-green:5000;

    location / {
        proxy_pass $upstream;
        proxy_set_header Host $host;
    }
}
